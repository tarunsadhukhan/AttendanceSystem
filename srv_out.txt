ins att 
    INSERT INTO daily_attendance
      (eb_id, attendance_date,
       attendance_source, attendance_type,
       attendance_mark, is_active, branch_id,
       spell, spell_hours, worked_department_id, worked_designation_id,
       status_id, working_hours, idle_hours)
    VALUES (%s, %s, %s, %s, %s, 1, %s, %s, %s, %s, %s, '3', %s, %s  )

GET MC 
    SELECT mm.machine_id id, CONCAT(mm.mech_code, ' ', mm.machine_name) AS name,
    mech_code , mech_posting_code machine_no
    FROM machine_mst mm
    LEFT JOIN mech_occu_link mol ON mm.machine_id = mol.mech_id
    WHERE mol.occu_id = %s order by mm.mech_code,mm.machine_name

Starting MyHrms Flask Server...
Database: sjm @ 13.126.47.172
Server ready at http://0.0.0.0:5051
 * Serving Flask app 'src'
 * Debug mode: on
ins att 
    INSERT INTO daily_attendance
      (eb_id, attendance_date,
       attendance_source, attendance_type,
       attendance_mark, is_active, branch_id,
       spell, spell_hours, worked_department_id, worked_designation_id,
       status_id, working_hours, idle_hours)
    VALUES (%s, %s, %s, %s, %s, 1, %s, %s, %s, %s, %s, '3', %s, %s  )

GET MC 
    SELECT mm.machine_id id, CONCAT(mm.mech_code, ' ', mm.machine_name) AS name,
    mech_code , mech_posting_code machine_no
    FROM machine_mst mm
    LEFT JOIN mech_occu_link mol ON mm.machine_id = mol.mech_id
    WHERE mol.occu_id = %s order by mm.mech_code,mm.machine_name

Starting MyHrms Flask Server...
Database: sjm @ 13.126.47.172
Server ready at http://0.0.0.0:5051
ins att 
    INSERT INTO daily_attendance
      (eb_id, attendance_date,
       attendance_source, attendance_type,
       attendance_mark, is_active, branch_id,
       spell, spell_hours, worked_department_id, worked_designation_id,
       status_id, working_hours, idle_hours)
    VALUES (%s, %s, %s, %s, %s, 1, %s, %s, %s, %s, %s, '3', %s, %s  )

GET MC 
    SELECT mm.machine_id id, CONCAT(mm.mech_code, ' ', mm.machine_name) AS name,
    mech_code , mech_posting_code machine_no
    FROM machine_mst mm
    LEFT JOIN mech_occu_link mol ON mm.machine_id = mol.mech_id
    WHERE mol.occu_id = %s order by mm.mech_code,mm.machine_name

Starting MyHrms Flask Server...
Database: sjm @ 13.126.47.172
Server ready at http://0.0.0.0:5051
Executing query to fetch companies... 
    SELECT
        co_id,
        co_name,
        co_logo
    FROM co_mst cm where cm.co_id in (2,153)
    ORDER BY co_name

Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
        
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30']Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
        
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30']Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
        
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30']


Department-wise: []
Department-wise: []
Department-wise: []
Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
         WHERE dm.branch_id = %s
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30', 103]
Department-wise: []
Executing query to fetch companies... 
    SELECT
        co_id,
        co_name,
        co_logo
    FROM co_mst cm where cm.co_id in (2,153)
    ORDER BY co_name

Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
        
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30']Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
        
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30']

Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
        
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30']
Department-wise: []
Department-wise: []Department-wise: []

Executing master query: 
            	SELECT sdm.sub_dept_id AS department_id,
                   sdm.sub_dept_desc AS department_name,
                   0  AS total_employees,
                   COUNT( da.eb_id) AS present
            FROM sub_dept_mst sdm
            LEFT JOIN dept_mst dm ON dm.dept_id = sdm.dept_id
            LEFT JOIN daily_attendance da
              ON da.worked_department_id  = sdm.sub_dept_id 
             AND da.attendance_date = %s
             AND da.is_active = 1
    
         WHERE dm.branch_id = %s
            GROUP BY sdm.sub_dept_id, sdm.sub_dept_desc
            ORDER BY sdm.sub_dept_desc
         with params ['2026-04-30', 103]
Department-wise: []
ins att 
    INSERT INTO daily_attendance
      (eb_id, attendance_date,
       attendance_source, attendance_type,
       attendance_mark, is_active, branch_id,
       spell, spell_hours, worked_department_id, worked_designation_id,
       status_id, working_hours, idle_hours)
    VALUES (%s, %s, %s, %s, %s, 1, %s, %s, %s, %s, %s, '3', %s, %s  )

GET MC 
    SELECT mm.machine_id id, CONCAT(mm.mech_code, ' ', mm.machine_name) AS name,
    mech_code , mech_posting_code machine_no
    FROM machine_mst mm
    LEFT JOIN mech_occu_link mol ON mm.machine_id = mol.mech_id
    WHERE mol.occu_id = %s order by mm.mech_code,mm.machine_name

Starting MyHrms Flask Server...
Database: sjm @ 13.126.47.172
Server ready at http://0.0.0.0:5051
ins att 
    INSERT INTO daily_attendance
      (eb_id, attendance_date,
       attendance_source, attendance_type,
       attendance_mark, is_active, branch_id,
       spell, spell_hours, worked_department_id, worked_designation_id,
       status_id, working_hours, idle_hours)
    VALUES (%s, %s, %s, %s, %s, 1, %s, %s, %s, %s, %s, '3', %s, %s  )

GET MC 
    SELECT mm.machine_id id, CONCAT(mm.mech_code, ' ', mm.machine_name) AS name,
    mech_code , mech_posting_code machine_no
    FROM machine_mst mm
    LEFT JOIN mech_occu_link mol ON mm.machine_id = mol.mech_id
    WHERE mol.occu_id = %s order by mm.mech_code,mm.machine_name

